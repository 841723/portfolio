---
import { defaultlocale, tcommons } from '@/i18n';
import type { Locale } from '@/types';

interface Props {
    title: string;
    id?: string;
}
const { title, id } = Astro.props;

const currentLocale = (Astro.currentLocale || defaultlocale) as Locale;
const t = tcommons(currentLocale);
---

<section id={id} class="flex flex-col gap-y-4 w-full max-w-[740px] mx-auto pt-20 px-2">
    <h2
        class="flex items-center gap-x-2 text-white text-3xl font-semibold"
    >
        <slot name="icon" />{t(title)}
    </h2>
    <slot />
</section>
